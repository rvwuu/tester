#+ Summary: this is just a test run
#+ Endpoint: https://druid.datalegend.net/rvanweeren/tester/services
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT * WHERE {
  ?sub ?pred ?obj .
} 
LIMIT 10
